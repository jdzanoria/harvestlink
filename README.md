# HarvestLink

Frontend-only React prototype for a Cebu farm-to-market marketplace. The app uses localStorage for users, products, purchase requests, and the current session.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
